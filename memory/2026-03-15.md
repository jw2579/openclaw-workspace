# 2026-03-15

- In the Discord `Job Tracker` thread, Jiaxuan reported that the LinkedIn cron was stuck/timing out and asked for a full rollback of the workspace to the latest GitHub version before **2026-03-14 17:56 EDT**.
- Identified the target pre-cutoff commit as `1b4d9e0285c19fb67e04d71b2ed19ed9d4610356` (`1b4d9e0`, 2026-03-14 21:09 UTC), then restored the workspace with `git reset --hard`, `git clean -fd`, and force-pushed `origin/master` back to that commit.
- Created a safety snapshot before rollback at `/root/.openclaw/backups/20260315T065550Z/`, including a copy of `~/.openclaw/cron` and a git bundle of the workspace.
- Cron state at takeover: 6 jobs total (1 main recurring LinkedIn job, 1 extra duplicate/stable job, and 4 failed one-shot LinkedIn job-post announce jobs). Cleaned it back down to 1 enabled recurring job.
- OpenClaw cron persistence lives outside the git repo in `~/.openclaw/cron/jobs.json` and `~/.openclaw/cron/jobs.json.bak`; the `.bak` file is only a rolling backup, not a reliable historical point-in-time restore.
- Restarted the OpenClaw Gateway successfully; `openclaw gateway status` showed the service running with RPC probe ok, though `openclaw status` still intermittently showed a gateway-timeout summary.
- Main root cause found after rollback: the cron definition did **not** roll back with the git repo. The recurring job `193d3733-4ef8-4e81-a0b7-c8462c882b9c` still had a newer payload that calls `scripts/linkedin_discord_messages.py` and tries to create per-job one-shot announce crons, but that formatter script does not exist in commit `1b4d9e0`.
- Conclusion from the investigation: the current timeout is **not** because the last-4-hours LinkedIn search returns too few jobs. The pipeline logic can return zero recommendations and should still complete. The timeout is due to a **cron payload / workspace version mismatch** after the repo rollback.
- The main recurring job was still configured on schedule `0 1,5,9,13,17,21 * * *` in `America/New_York`, but its last completed good runs had selected only 3-4 jobs and still finished normally, reinforcing that low result count is not the blocker.
