# 2026-03-14

- Worked on Jiaxuan's LinkedIn job pipeline in the Discord #job-alert thread.
- Durable pipeline changes completed and already pushed in git commit `4edd954d8f33dc677a9631006169c35243171183`:
  - Notion database schema simplified to decision-focused columns: Name, position, URL, Status, Score, Match reason, Location, Work mode, Posted.
  - Removed the Notion `note` column and deleted the AI-summary note logic.
  - Added posted-time extraction, star-based scoring, invalid LinkedIn-link filtering, health/biotech weighting, Notion URL dedupe, and increased cron timeout from 600s to 1200s.
- The only cron job for this thread remains `linkedin-software-dev-4h` (`193d3733-4ef8-4e81-a0b7-c8462c882b9c`). Its schedule anchor was preserved; next run observed in this session was 2026-03-14 08:32:54 GMT / 04:32:54 EDT.
- Confirmed `gog` has a default authenticated Gmail account configured for Jiaxuan, and `gog gmail send --dry-run` works.
- Added new uncommitted local code to generate a PDF version of the Markdown report and email it via Gmail when `LINKEDIN_EMAIL_REPORTS=1` is set.
- Real end-to-end Gmail/PDF test results:
  - PDF generation succeeded (custom pure-Python PDF writer; no external Markdown/PDF package installed).
  - Gmail send failed with `Google API error (403 insufficientPermissions): Request had insufficient authentication scopes.`
  - This means the current `gog` OAuth token does not have enough Gmail send scope for real outbound mail, even though the account is present and dry-run works.
- Important next step: re-auth or expand `gog` Gmail OAuth scopes for Jiaxuan's account so cron-based self-emails with PDF attachments can actually send.
- Later the Gmail re-auth succeeded with `gmail.send` + `drive` scopes; real test email sending to `jiaxuan.wu18@gmail.com` worked.
- Drive upload remained blocked by Google project configuration (`403 accessNotConfigured`: Drive API disabled for the gog OAuth client project), so the working delivery path is: email the PDF to Jiaxuan and expose the Gmail thread URL in cron output instead of a Drive link.
- Pipeline behavior was changed so cron runs with `LINKEDIN_EMAIL_REPORTS=1` and `LINKEDIN_SAVE_LOCAL_REPORTS=0`, meaning reports/PDFs are emailed and not persisted locally on the VPS by default.
- Jiaxuan later requested high/low peak behavior by New York time using the single existing cron schedule (`00,04,08,12,16,20` daily):
  - `high_peak` = weekdays 09:00-21:00 → fetch 100, recommend up to 10 (same as before)
  - `low_peak` = weekdays 21:00-09:00 plus the full weekend window → fetch 50, recommend up to 5 only if they pass a quality threshold; otherwise recommend none.
